import React from "react";

const Heading = ({ title }) => {
  return (
    <div className=" w-full font-bold text-4xl text-center rounded-sm ">
      <h2>{title}</h2>
    </div>
  );
};

export default Heading;
